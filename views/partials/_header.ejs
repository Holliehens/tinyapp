<header>
  <nav class="navbar navbar-expand-md navbar-dark bg-success">
    <a class="navbar-brand" href="/urls">TinyApp</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
      </button>
      <!-- "locals.user" (locals.whatever) will evaluate to undefined to prevent error/crash.
      If just "user" it will refer to a variable and we cannot refer to a variable that is not defined -->
      <% if (locals.user) {%>
      <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
        <div class="navbar-nav">
          <a class="nav-item nav-link" href="/urls">My URLs</a>
          <a class="nav-item nav-link" href="/urls/new">Creat New URL</a>

          <form method="POST" action="/logout">
            <button type="submit" class="btn btn-primary">Logout</button>
          </form>
          <p>
            Welcome <%=user.email%>! 
            You Are Logged In
          </p>
        </div>
        <% } else {%>
          <form   method="GET" action="/login">
          <button type="submit" class="btn btn-primary">Login</button>
        </form>
      <% } %>
      </div>
    </a>
  </nav>
</header>